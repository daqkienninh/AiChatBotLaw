services:
  - type: web
    name: embed-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python embed_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
